name: build-api-image

on:
  push:
    branches: [main]
    paths:
      - 'api/**'
      - '.github/workflows/api-image.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build & push
        uses: docker/build-push-action@v6
        with:
          context: ./api
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/painterapp-api:latest
            ghcr.io/${{ github.repository_owner }}/painterapp-api:${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
